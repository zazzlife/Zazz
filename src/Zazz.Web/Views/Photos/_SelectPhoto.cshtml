@using PagedList.Mvc
@model PagedList.IPagedList<PhotoViewModel>
@{
    Layout = null;
}
<div style="margin-top: 25px;" class="pic-list">
        @foreach (var p in Model)
        {
                <div class="img" style="width: 115px; height: 115px;">
                    <img src="@p.PhotoUrl.SmallLink" title="Click on photo to select it"  data-selectPhoto="1" data-id="@p.PhotoId" data-url="@p.PhotoUrl.SmallLink" />
                </div>
        }
    
    <div class="clearfix"></div>

    <div class="text-center" data-ajax-pagination="1" data-update="#pg-photos">
        @Html.PagedListPager(Model, page => String.Format("/photos/getphotos/?albumId={0}&page={1}", ViewBag.AlbumId, page))
    </div>
</div>
