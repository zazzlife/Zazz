@using Zazz.Core.Models.Data.Enums
@model UserProfileViewModel
@{
    ViewBag.Title = Model.UserName;
}

<div class="container">
    @*COVER IMAGE*@
    <img id="coverImg" class="img-polaroid" src="@Model.CoverPhotoUrl" />

    <div style="min-height: 120px;">

        @*LEFT ITEMS*@
        <div class="pull-left">
            <table class="club-details">
                <tbody>
                    <tr>
                        <td><i class="icon-glass"></i></td>
                        <td>Night Club</td>
                    </tr>
                    <tr>
                        <td><i class="icon-building"></i></td>
                        <td>Victoria B.C.</td>
                    </tr>
                    <tr>
                        <td><i class="icon-map-marker" style="margin-left: 1px;"></i></td>
                        <td>751 View street</td>
                    </tr>
                </tbody>
            </table>
        </div>

        @*RIGHT ITEMS*@
        <div class="pull-right">
            
            <div class="club-party-web-info text-center">
                
                <i class="badge badge-info followers-count">5</i>
                <br/>
                <div>Party Web</div>
                <button data-action="follow" data-id="@Model.UserId"
                     class="btn btn-info btn-follow"><i class="icon-plus"></i></button>
            </div>
            

        </div>

        @*CENTER ITEMS*@
        <div class="profile-center">
            <img id="profilePic" class="img-polaroid img-rounded" src="@Model.UserPhotoUrl.SmallLink" />
            <h3>@Model.UserName</h3>
        </div>
    </div>

    <div class="clearfix"></div>
    
    
    <div class="weeklies-container">
        <div class="weeklies-text">
            <a href="#" style="position: absolute;"><strong>Add</strong></a>
            <div>Weeklies</div>
        </div>
        <ul class="weeklies-items">
            
            @for (int i = 0; i < 7; i++)
            {
                <li class="weekly">
                    <img src="~/Images/placeholder.gif"/>
                    <div>Mon</div>
                </li>
            }
        </ul>
        <div class="special-events">
            <div class="special-events-count badge badge-important">5</div>
            <div class="special-events-text" style="margin-bottom: 18px;">Special</div>
            <div class="special-events-text">Events</div>

        </div>
    </div>
    <div class="clearfix"></div>
    

    @* TABS *@
    @if (Model.IsSelf)
    {
        <div>
            @Html.Partial("_PostInputsPartial")
        </div>
    }
    else
    {
        <div id="profileTabs">
            <textarea data-tag="1" placeholder="Post something on @Model.UserName's wall" id="postInput"></textarea>
            <button type="button" class="btn btn-primary" id="submitPostBtn" data-touser="@Model.UserId">Submit</button>
        </div>
    }
    
    @* Photos List *@
    
    <div class="well party-albums">
        <h6>Party Albums</h6>
        
        @for (int i = 0; i < 3; i++)
        {
            <strong>@DateTime.UtcNow.AddDays(i * -1).ToLongDateString()</strong>
            <ul class="party-images">
                @for (int j = 0; j < 13; j++)
                {
                    <li><img src="~/Images/placeholder.gif"/></li>
                }
            </ul>
        }
        
        <div class="text-center"><a href="#">See more...</a></div>
    </div>


    @* FEEDS *@

    <div id="feedContent">
        @Html.Partial("_FeedsPartial", Model.Feeds)
    </div>

    <div class="clearfix"></div>
</div>
@Html.Partial("_UploadPicModal",  new UploadPicModalModel { ModalId = "uploadPicModalWithFeed", CreateFeed = true })

