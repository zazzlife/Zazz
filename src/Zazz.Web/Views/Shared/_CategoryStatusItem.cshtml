@model CategoryStatViewModel

@{
    var side = Model.IsRightSide ? "right" : "left";
}

<div class="category category-@side">
    <a href="@Url.Action("Categories", "Home", new { @select=Model.CategoryName })">
        <img src="@String.Format("/Images/category-boxes/{0}/{1}.png", side, Model.CategoryName)" class="category-img" />
    </a>
    <div class="category-text">
        <a href="@Url.Action("Categories", "Home", new { @select=Model.CategoryName })"><h5 class="category-text-header">@Model.CategoryName</h5></a>
        <p>@Model.UsersCount @(Model.UsersCount == 1 ? "person is" : "people are") talking about this.</p>
    </div>
</div>
